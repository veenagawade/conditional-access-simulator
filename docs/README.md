# docs/

Project documentation lives here so it is versioned alongside the code.

| File | What it is |
|---|---|
| `github-netlify-setup.md` | Repo + deployment setup, GitHub Desktop only (no terminal) |
| `phase1-spec.md` | **Paste your finished Phase 1 design spec here** — attributes, rule structure, decision logic, starter rules |
| `roadmap.md` | Phases 0–6 |

Keeping the spec in the repo means every future prompt to Claude can start with
"read `docs/phase1-spec.md`" instead of re-explaining the design from memory.

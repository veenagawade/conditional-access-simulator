# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp

# Node (for when you add tooling later)
node_modules/
npm-debug.log*
dist/
build/

# Netlify
.netlify/

# Secrets — never commit these. If you ever add an API key, it goes in
# Netlify's environment variables UI, not in the repo.
.env
.env.*
!.env.example
*.pem
*.key
